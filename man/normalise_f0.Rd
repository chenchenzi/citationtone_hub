% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalise.R
\name{normalise_f0}
\alias{normalise_f0}
\title{Normalise f0 by speaker}
\usage{
normalise_f0(
  data,
  f0 = "f0",
  speaker = "speaker",
  tone = "tone",
  method = c("semitone", "zscore"),
  mean_method = c("weighted", "simple")
)
}
\arguments{
\item{data}{A data frame containing at least the f0, speaker, and tone
columns.}

\item{f0}{Name of the column with raw f0 values (Hz). Default \code{"f0"}.}

\item{speaker}{Name of the speaker ID column. Default \code{"speaker"}.}

\item{tone}{Name of the tone category column. Default \code{"tone"}. Used only
when \code{mean_method = "weighted"}.}

\item{method}{Normalisation formula. One of:
\itemize{
\item \code{"semitone"} (default): \code{ST = 12 * log2(f0 / speaker_mean)}.
\item \code{"zscore"}: \code{Z = (f0 - speaker_mean) / sd(f0_speaker)}.
}}

\item{mean_method}{How the per-speaker mean is computed. One of
\code{"weighted"} (default) or \code{"simple"}. See Details.}
}
\value{
The input data frame with two added columns:
\itemize{
\item \code{speaker_mean} — the per-speaker mean used for normalisation
\item \code{f0_st} (if \code{method = "semitone"}) or \code{f0_zscore} (if \code{method = "zscore"})
}
}
\description{
Add a normalised f0 column to \code{data}, computed per speaker. Two methods
are available: semitones referenced on a per-speaker mean (perceptually
uniform), or per-speaker z-scores (statistical centring and scaling).
}
\details{
Per-speaker means can be computed in two ways:
\itemize{
\item \code{"weighted"} (default): mean of per-tone means. Each tone contributes
equally to the centre of the speaker's tonal space, regardless of how
many tokens it has. Preferred for unbalanced designs.
\item \code{"simple"}: arithmetic mean of all f0 observations for that speaker.
}
}
\examples{
df <- data.frame(
  speaker = rep(c("S01", "S02"), each = 6),
  tone    = rep(c("T1", "T2", "T3"), times = 4),
  f0      = c(150, 180, 130, 160, 190, 140,
              200, 240, 170, 210, 250, 175)
)
head(normalise_f0(df, method = "semitone"))
head(normalise_f0(df, method = "zscore"))

}
