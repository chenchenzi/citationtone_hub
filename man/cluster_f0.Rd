% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster_f0}
\alias{cluster_f0}
\title{Cluster f0 contours into candidate tone categories}
\usage{
cluster_f0(
  feat,
  method = c("kmeans", "hclust", "gmm"),
  k,
  nstart = 25L,
  hclust_method = "ward.D2"
)
}
\arguments{
\item{feat}{A list from \code{\link[=cluster_features]{cluster_features()}} (needs \code{features}+\code{contours}).}

\item{method}{\code{"kmeans"}, \code{"hclust"} (Ward), or \code{"gmm"} (model-based,
needs \code{mclust}).}

\item{k}{Number of clusters.}

\item{nstart}{k-means restarts. Default 25.}

\item{hclust_method}{Linkage for \code{"hclust"}. Default \code{"ward.D2"}.}
}
\value{
A list with \code{assignment} (named integer per token), \code{method}, \code{k},
\code{sizes}, \code{cluster_means} (k x n_points mean contour per cluster),
\code{tokens}, \code{uncertainty} (per-token 1 - max posterior, GMM only; else
\code{NULL}), and \code{tree} (the fitted \code{hclust} object for method \code{"hclust"},
else \code{NULL}).
}
\description{
Cluster f0 contours into candidate tone categories
}
\references{
Kaland, C. (2023). Contour clustering: A field-data-driven approach for
documenting and analysing prototypical f0 contours. \emph{Journal of the
International Phonetic Association}, 53(1), 159--188.
\doi{10.1017/S0025100321000049}

Hartigan, J. A., & Wong, M. A. (1979). Algorithm AS 136: A k-means
clustering algorithm. \emph{Applied Statistics}, 28(1), 100--108.

Ward, J. H. (1963). Hierarchical grouping to optimize an objective
function. \emph{Journal of the American Statistical Association}, 58(301),
236--244.

Scrucca, L., Fop, M., Murphy, T. B., & Raftery, A. E. (2016). mclust 5:
Clustering, classification and density estimation using Gaussian finite
mixture models. \emph{The R Journal}, 8(1), 289--317.
}
