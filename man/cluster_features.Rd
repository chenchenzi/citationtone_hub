% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{cluster_features}
\alias{cluster_features}
\title{Build per-token feature vectors for f0-contour clustering}
\usage{
cluster_features(
  data,
  f0 = "f0",
  token = "token",
  time = "time",
  speaker = NULL,
  tone = NULL,
  n_points = 20L,
  features = c("points", "legendre", "dct", "derivative"),
  degree = 4L,
  register = c("level", "shape")
)
}
\arguments{
\item{data}{Long-format data frame, one row per f0 sample.}

\item{f0, token, time}{Column names. \code{time} is normalised per token.}

\item{speaker}{Optional speaker column, carried through as metadata.}

\item{tone}{Optional tone column, carried through for later validation.}

\item{n_points}{Number of points to resample each contour to. Default 20.}

\item{features}{Feature representation: \code{"points"} (the resampled contour),
\code{"legendre"} (orthogonal-polynomial coefficients of \code{degree}), \code{"dct"}
(low-order discrete-cosine coefficients), or \code{"derivative"} (first
difference: cluster by rate of change / movement, discarding height).}

\item{degree}{Order for \code{"legendre"}/\code{"dct"} features. Default 4.}

\item{register}{\code{"level"} keeps each contour's height (so high vs low level
tones separate); \code{"shape"} centres each contour to mean 0 (cluster on
shape only).}
}
\value{
A list with \code{features} (token x p matrix used for clustering),
\code{contours} (token x n_points resampled f0, for plotting cluster means),
\code{tokens}, and \code{meta} (token / speaker / tone).
}
\description{
Resamples every token's f0 contour to a common length and turns it into a
fixed-length feature vector suitable for clustering. f0 should already be
speaker-normalised (e.g. \code{f0_st} from \code{\link[=normalise_f0]{normalise_f0()}}) so that clusters
reflect tone shape and register rather than who has a high voice.
}
