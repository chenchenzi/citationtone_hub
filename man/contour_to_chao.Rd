% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chao.R
\name{contour_to_chao}
\alias{contour_to_chao}
\title{Convert per-tone contours to Chao tone numerals}
\usage{
contour_to_chao(
  contour_data,
  tone_col = "tone",
  time_col = "time",
  f0_col = "f0_predicted",
  threshold = 0.5,
  raw_data = NULL,
  raw_token = "token",
  raw_f0 = "f0",
  raw_tone = "tone"
)
}
\arguments{
\item{contour_data}{Per-tone contour data, typically from
\code{\link[=compute_mean_contour]{compute_mean_contour()}}, \code{\link[=predict_gca]{predict_gca()}}, or \code{\link[=predict_gamm]{predict_gamm()}}.
Must contain \code{tone}, \code{time}, and \code{f0_predicted} columns (or rename
via the \code{tone_col}, \code{time_col}, \code{f0_col} arguments).}

\item{tone_col, time_col, f0_col}{Column names within \code{contour_data}.}

\item{threshold}{Chao-units threshold for turning-point detection.
Default \code{0.5}. See Details.}

\item{raw_data}{Optional long-format raw data, used to compute the
robust FOR. If \code{NULL}, the \code{robust} column is left as \code{NA}.}

\item{raw_token, raw_f0, raw_tone}{Column names in \code{raw_data}.}
}
\value{
A data frame with one row per tone, containing:
\itemize{
\item \code{tone}: tone label.
\item \code{n_digits}: number of digits in the numeral (\code{2} or \code{3}).
\item \code{refline}: reference-line method numeral, e.g. \code{"55"}.
\item \code{interval}: interval-based numeral, e.g. \code{"45"}.
\item \code{robust}: robust FOR numeral (or \code{NA} if \code{raw_data} is not given).
\item \code{shape}: shape name from \code{\link[=classify_contour]{classify_contour()}} applied to \code{refline}.
\item \code{sample_times}, \code{ref_continuous}, \code{int_continuous},
\code{rob_continuous}: list columns of the per-sample numeric values
underlying the digit strings, useful for plotting or further
analysis.
}

Two attributes are attached to the returned data frame: \code{f0_min} and
\code{f0_max} of the input contour, plus a \code{robust_stats} list containing
the means and standard deviations used for the robust method (or
\code{NULL} if \code{raw_data} was not supplied).
}
\description{
Summarises per-tone contours as Chao tone numerals (Chao 1930): 5-level
digit strings like \code{"55"}, \code{"35"}, or \code{"214"} that describe each
contour by its endpoints and any interior turning point. Three
conversion methods are computed in one pass so the analyst can
compare them or pick whichever is most appropriate for the corpus
(Xu & Zhang 2024).
}
\details{
\subsection{The three conversion methods}{

All three use Fraction of Range (FOR) normalisation, which maps f0
onto a bounded scale representing the speaker's (or corpus's) pitch
range. They differ in how the range is defined and how scaled values
are mapped to Chao digits.
\itemize{
\item \strong{Reference-line FOR on \verb{[1, 5]}}:
\verb{f0' = (f0 - f0_min) / (f0_max - f0_min) * 4 + 1}, then rounded.
Five reference lines at integer values; each sampled point snaps
to the nearest integer.
\item \strong{Interval-based FOR on \verb{(0, 5]}}:
\verb{f0' = (f0 - f0_min) / (f0_max - f0_min) * 5}, then ceilinged.
Five equal-width intervals; each sampled point is assigned to its
interval via \code{ceiling()}.
\item \strong{Robust FOR on \verb{(0, 5]}} (only if \code{raw_data} is supplied):
same interval formula, but the range is defined by μ ± σ of
per-token f0 peaks from the highest-pitched tone (max) and
per-token f0 valleys from the lowest-pitched tone (min), following
Zhu (1999) and the recommendation of Xu & Zhang (2024). More
robust to outliers than using the corpus-wide min and max.
}
}

\subsection{Turning-point detection}{

For each tone, the function samples the contour either at its two
endpoints (yielding a 2-digit numeral) or at the two endpoints plus
one interior turning point (yielding a 3-digit numeral). The 3-digit
case applies when either:
\itemize{
\item The interior minimum is more than \code{threshold} Chao units below
both endpoints (a \emph{dip}), or
\item The interior maximum is more than \code{threshold} Chao units above
both endpoints (a \emph{peak}).
}

The default \code{threshold = 0.5} (half a Chao unit on the \verb{[1, 5]}
scale) is a conservative choice. Lower thresholds produce more
3-digit numerals, useful for tonally rich languages.
}
}
\examples{
data(sample_f0)
mc <- compute_mean_contour(sample_f0,
                           token = "token", f0 = "f0_Hz",
                           time  = "time",  tone = "tone")
chao <- contour_to_chao(mc,
                        raw_data  = sample_f0,
                        raw_token = "token",
                        raw_f0    = "f0_Hz",
                        raw_tone  = "tone")
chao[, c("tone", "refline", "interval", "robust", "shape")]

}
\references{
Chao, Y. R. (1930). A system of tone-letters. \emph{Le Maître
Phonétique}, 30, 24–27.

Zhu, X. (1999). \emph{Shanghai Tonetics}. LINCOM Europa.

Xu, C., & Zhang, C. (2024). A cross-linguistic review of citation
tone production studies: Methodology and recommendations.
\emph{The Journal of the Acoustical Society of America}, 156(4),
2538–2565. \doi{10.1121/10.0032356}
}
\seealso{
\itemize{
\item \code{\link[=compute_mean_contour]{compute_mean_contour()}} for one common upstream step.
\item \code{\link[=classify_contour]{classify_contour()}} for the shape classification helper.
\item \code{\link[=predict_gca]{predict_gca()}} and \code{\link[=predict_gamm]{predict_gamm()}} for model-based contour
sources.
}
}
