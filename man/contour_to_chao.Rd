% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chao.R
\name{contour_to_chao}
\alias{contour_to_chao}
\title{Convert per-tone contours to Chao tone numerals}
\usage{
contour_to_chao(
  contour_data,
  tone_col = "tone",
  time_col = "time",
  f0_col = "f0_predicted",
  threshold = 0.5,
  raw_data = NULL,
  raw_token = "token",
  raw_f0 = "f0",
  raw_tone = "tone"
)
}
\arguments{
\item{contour_data}{Per-tone contour data, typically from
\code{\link[=compute_mean_contour]{compute_mean_contour()}}, \code{\link[=predict_gca]{predict_gca()}}, or \code{\link[=predict_gamm]{predict_gamm()}}. Must
contain \code{tone}, \code{time}, and \code{f0_predicted} columns (or rename via the
\code{tone_col}, \code{time_col}, \code{f0_col} arguments).}

\item{tone_col, time_col, f0_col}{Column names within \code{contour_data}.}

\item{threshold}{Chao-units threshold for turning-point detection.
Default \code{0.5}.}

\item{raw_data}{Optional long-format raw data, used to compute the
robust FOR. If \code{NULL}, the \code{robust} column is left as \code{NA}.}

\item{raw_token, raw_f0, raw_tone}{Column names in \code{raw_data}.}
}
\value{
A data frame with one row per tone, containing:
\itemize{
\item \code{tone} — tone label
\item \code{n_digits} — number of digits in the numeral (2 or 3)
\item \code{refline} — reference-line method numeral, e.g. \code{"55"}
\item \code{interval} — interval-based numeral, e.g. \code{"45"}
\item \code{robust} — robust FOR numeral (or \code{NA} if \code{raw_data} not given)
\item \code{shape} — shape name from \code{\link[=classify_contour]{classify_contour()}} applied to \code{refline}
\item \code{sample_times}, \code{ref_continuous}, \code{int_continuous}, \code{rob_continuous}
— list columns of the per-sample numeric values
}
}
\description{
For each tone in \code{contour_data}, sample the contour at its turning
points (or at the endpoints if no turning point is detected), then map
each sample to a Chao digit (1-5) using up to three methods:
}
\details{
\itemize{
\item \strong{Reference-line FOR \verb{[1, 5]}} — \verb{f0' = (f0 - f0_min) / (f0_max - f0_min) * 4 + 1},
rounded.
\item \strong{Interval-based FOR \verb{(0, 5]}} — \verb{f0' = (f0 - f0_min) / (f0_max - f0_min) * 5},
ceiling.
\item \strong{Robust FOR \verb{(0, 5]}} (optional) — same formula but with the range
defined by \verb{mu ± sigma} of per-token f0 peaks from the highest-pitched
tone (max) and per-token f0 valleys from the lowest-pitched tone
(min). Computed only if \code{raw_data} is supplied.
}

The turning-point detection uses a Chao-units \code{threshold} (default
\code{0.5}) on the \verb{[1, 5]} reference scale: a tone gets 3 digits if either
a dip or peak exceeds the threshold relative to its endpoints,
otherwise 2 digits.
}
