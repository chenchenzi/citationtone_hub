% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/curate.R
\name{apply_relabels}
\alias{apply_relabels}
\title{Apply tone re-labels and exclusions to a long-format f0 table}
\usage{
apply_relabels(
  data,
  token = "token",
  tone = "tone",
  relabel = NULL,
  exclude = character(0),
  note = NULL
)
}
\arguments{
\item{data}{A long-format data frame with one row per f0 sample.}

\item{token}{Column name of token ID. Default \code{"token"}.}

\item{tone}{Column name of the (original) tone category. Default \code{"tone"}.}

\item{relabel}{A named character vector mapping token IDs (names) to their
new tone label (values); or \code{NULL} for no relabelling. Tokens not named
keep their original tone.}

\item{exclude}{A character vector of token IDs to mark as excluded; or an
empty vector for none.}

\item{note}{A named character vector mapping token IDs (names) to a
free-text reason for the curation decision (e.g. \code{"literary form"},
\code{"sandhi form"}); or \code{NULL} for none. Tokens not named get \code{NA}.}
}
\value{
\code{data} with three appended columns:
\itemize{
\item \code{tone_relabelled}: character, the curated tone label (original tone where
no relabel applies).
\item \code{excluded}: logical, \code{TRUE} for tokens in \code{exclude}.
\item \code{curate_note}: character, the reason recorded for a token (or \code{NA}).
}
}
\description{
Adds annotation-curation columns to a long-format f0 data frame without
touching the original tone labels. Tokens named in \code{relabel} receive a new
value in \code{tone_relabelled}; tokens named in \code{exclude} are marked \code{TRUE} in
\code{excluded}. All other tokens keep their original tone in \code{tone_relabelled}
and \code{FALSE} in \code{excluded}. Curation operates at the token level: every row
belonging to a token receives that token's relabel / exclusion.
}
\details{
The original \code{tone} column is never modified, so a curation decision is
always reversible and auditable. Downstream analyses can then either group
by \code{tone_relabelled} (treating a variant as its own category, e.g.
\code{T4-colloquial} vs \code{T4-literary}) and/or drop rows where \code{excluded} is
\code{TRUE}.

This is an annotation step, not a signal-repair step: it assumes the f0
values are already clean (see \code{\link[=inspect_f0]{inspect_f0()}} and the F0 Correction tab for
repairing mis-tracked f0). Variant patterns are often discovered visually
or surfaced by the token-level register check of \code{\link[=flag_level_outliers]{flag_level_outliers()}},
which flags tokens whose overall level is unusual for their speaker and
tone --- a natural seed for relabelling.
}
\examples{
data(sample_f0)
# Relabel two tokens to a literary-reading variant, exclude one mis-elicited
out <- apply_relabels(sample_f0,
                      token   = "token",
                      tone    = "tone",
                      relabel = c("t4_03" = "T4lit", "t4_07" = "T4lit"),
                      exclude = c("t5_11"))
table(out$tone, out$tone_relabelled, useNA = "ifany")

}
\seealso{
\code{\link[=flag_level_outliers]{flag_level_outliers()}} for the register check that seeds variant
discovery; \code{\link[=inspect_f0]{inspect_f0()}} for the full diagnostic pass.
}
