% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect.R
\name{flag_outliers}
\alias{flag_outliers}
\title{Flag per-token f0 outliers using by-speaker z-scores}
\usage{
flag_outliers(
  data,
  f0 = "f0",
  token = "token",
  speaker = "speaker",
  z_threshold = 3
)
}
\arguments{
\item{data}{A long-format data frame, one row per f0 sample.}

\item{f0}{Column name of f0 (Hz). Default \code{"f0"}.}

\item{token}{Column name of token ID. Default \code{"token"}.}

\item{speaker}{Column name of speaker ID. Default \code{"speaker"}.}

\item{z_threshold}{Absolute z-score above which a token is flagged.
Default \code{3}, covering 99.7\% of a normal distribution.}
}
\value{
A \emph{token-level} data frame (one row per token) with columns:
\code{f0_token_max}, \code{f0_token_min}, \code{f0_token_mean}, \code{f0_token_sd},
\code{z_max}, \code{z_min}, \code{flag_too_high}, \code{flag_too_low}, plus the original
\code{token} and \code{speaker} columns.
}
\description{
For each token (a unique value of the \code{token} column), compute the maximum
and minimum f0. Within each speaker, z-score those per-token extremes. A
token is flagged whenever the absolute z-score of either its max or its
min exceeds \code{z_threshold}.
}
\examples{
df <- data.frame(
  token   = rep(c("t1", "t2", "t3"), each = 4),
  speaker = rep("S01", 12),
  f0      = c(150, 160, 155, 158,
              152, 159, 154, 157,
              400, 410, 405, 408)   # t3 is clearly a tracking error
)
flag_outliers(df, z_threshold = 1.5)

}
