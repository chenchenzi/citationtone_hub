% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cluster.R
\name{choose_k_f0}
\alias{choose_k_f0}
\title{Diagnostics for choosing the number of clusters (candidate tones)}
\usage{
choose_k_f0(feat, k_range = 2:8, nstart = 25L, gap = TRUE, bending = 1)
}
\arguments{
\item{feat}{A list from \code{\link[=cluster_features]{cluster_features()}}, or a numeric feature matrix.}

\item{k_range}{Integer vector of cluster counts to evaluate. Default \code{2:8}.}

\item{nstart}{k-means restarts. Default 25.}

\item{gap}{Whether to compute the gap statistic (needs the \code{cluster}
package). Default TRUE.}

\item{bending}{MDL bending factor (>= 1; Kaland & Ellison 2023). Higher
values down-weight the model/residual cost so fewer clusters are preferred;
tune until the MDL (information-cost) curve is U-shaped. Default 1.}
}
\value{
A list with \code{table} (k, wss, silhouette, gap, gap_se, mdl),
suggestions \code{k_silhouette}, \code{k_gap}, \code{k_gmm}, \code{k_mdl} (any may be \code{NA}),
and \code{assignments} (the per-k k-means cluster vectors).
}
\description{
Runs k-means across a range of \code{k} and reports the elbow (total
within-cluster sum of squares), average silhouette width, and -- when the
\code{cluster} / \code{mclust} packages are available -- the gap statistic and the
GMM/BIC-preferred \code{k}. No single index is authoritative; use the spread to
read off a plausible \emph{range} for the number of tones.
}
\references{
Rousseeuw, P. J. (1987). Silhouettes: A graphical aid to the interpretation
and validation of cluster analysis. \emph{Journal of Computational and
Applied Mathematics}, 20, 53--65.

Tibshirani, R., Walther, G., & Hastie, T. (2001). Estimating the number of
clusters in a data set via the gap statistic. \emph{Journal of the Royal
Statistical Society B}, 63(2), 411--423.

Kaland, C., & Ellison, T. M. (2023). Evaluating cluster analysis on f0
contours: An information theoretic approach on three languages.
\emph{Proceedings of the 20th International Congress of Phonetic Sciences},
3448--3452.
}
