% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect.R
\name{flag_level_outliers}
\alias{flag_level_outliers}
\title{Flag tokens whose overall f0 level is unusual for their speaker and tone}
\usage{
flag_level_outliers(
  data,
  f0 = "f0",
  token = "token",
  speaker = "speaker",
  tone = "tone",
  level_threshold = 3.5,
  min_tokens = 4
)
}
\arguments{
\item{data}{A long-format data frame with one row per f0 sample.}

\item{f0}{Column name of f0 in Hz. Default \code{"f0"}.}

\item{token}{Column name of token ID. Default \code{"token"}.}

\item{speaker}{Column name of speaker ID. Default \code{"speaker"}.}

\item{tone}{Column name of tone category. Default \code{"tone"}.}

\item{level_threshold}{Absolute modified z-score above which a token's
level is flagged. Default \code{3.5} (Iglewicz & Hoaglin 1993).}

\item{min_tokens}{Minimum number of same-speaker-same-tone tokens
required to run the check for a group. Default \code{4}.}
}
\value{
A token-level data frame (one row per token with at least one
voiced sample) with columns: \code{f0_token_median}, \code{level_st},
\code{n_tone_peers}, \code{level_modz}, \code{flag_level_high}, \code{flag_level_low},
plus the original \code{token}, \code{speaker}, and \code{tone} columns.
}
\description{
Detects tokens whose \emph{whole contour} sits too high or too low compared
with other tokens of the \strong{same speaker and same tone}, even when the
contour is smooth (no sample-to-sample jumps) and not extreme for the
speaker overall. This catches the "right shape, wrong height" case that
\code{\link[=flag_outliers]{flag_outliers()}} (which pools all tones within a speaker) and
\code{\link[=flag_pitch_jumps]{flag_pitch_jumps()}} (which only sees within-token movement) both miss
--- for example a low-tone token mis-tracked up into the mid-tone band.
}
\details{
\subsection{What the function does internally}{
\enumerate{
\item Drop samples where \code{f0} is \code{NA} or \code{0} (unvoiced frames).
\item Compute each token's \strong{median f0}, converted to semitones
(\code{12 * log2(Hz)}). The median (rather than the mean) is used so that a
handful of mis-tracked frames --- which \code{\link[=flag_pitch_jumps]{flag_pitch_jumps()}} already
catches --- do not shift a token's apparent level.
\item Within each speaker x tone group, compute the \strong{modified z-score}
(Iglewicz & Hoaglin 1993) of each token's level:
\code{0.6745 * (level - median) / MAD}, where MAD is the median absolute
deviation. The modified z-score is on the same scale as an ordinary
z-score but is built from the robust median/MAD, so a few deviant
tokens cannot inflate the spread and mask themselves. When the MAD is
zero (more than half the peers share an identical level), the function
falls back to the mean-absolute-deviation form,
\code{(level - median) / (1.2533 * meanAD)}.
\item Flag a token as \verb{level too high} / \verb{level too low} when its modified
z-score exceeds \code{+level_threshold} / falls below \code{-level_threshold}.
}
}

\subsection{Minimum group size}{

A spread cannot be estimated from a near-empty group, so the check only
runs for speaker x tone groups with at least \code{min_tokens} tokens; tokens
in smaller groups are returned with \code{level_modz = NA} and are never
flagged.

This is a token-level cleaning step intended to run on \strong{raw f0 (Hz)
before normalisation}: the within speaker x tone comparison is
self-normalising, and cleaning before normalisation keeps mis-tracked
tokens from corrupting the by-speaker normalisation statistics.
}
}
\examples{
data(sample_f0)
lvl <- flag_level_outliers(sample_f0,
                           f0      = "f0_Hz",
                           token   = "token",
                           speaker = "speaker",
                           tone    = "tone")
table(lvl$flag_level_high, lvl$flag_level_low, useNA = "ifany")

}
\references{
Iglewicz, B., & Hoaglin, D. C. (1993). \emph{How to Detect and Handle
Outliers}. ASQC Quality Press.

Xu, C., & Zhang, C. (2024). A cross-linguistic review of citation
tone production studies: Methodology and recommendations.
\emph{The Journal of the Acoustical Society of America}, 156(4),
2538–2565. \doi{10.1121/10.0032356}
}
\seealso{
\itemize{
\item \code{\link[=flag_outliers]{flag_outliers()}} for pooled per-speaker max/min outliers.
\item \code{\link[=flag_pitch_jumps]{flag_pitch_jumps()}} for sample-level artefact detection.
\item \code{\link[=inspect_f0]{inspect_f0()}} for the wrapper that runs all three.
}
}
