% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_app.R
\name{run_app}
\alias{run_app}
\title{Launch the Shinytone app locally}
\usage{
run_app(...)
}
\arguments{
\item{...}{Additional arguments forwarded to \code{\link[shiny:runApp]{shiny::runApp()}} — e.g.
\code{port = 4321}, \code{launch.browser = FALSE}, \code{host = "0.0.0.0"}.}
}
\value{
Does not return; runs until the app is stopped.
}
\description{
Boots the bundled Shiny application that ships inside the installed
package. Equivalent to the hosted version at
\url{https://chenzixu.shinyapps.io/shinytone/} but running on your own
machine, so it has no file-size, memory, or session limits and your
audio / CSV files never leave your computer.
}
\details{
GitHub-installed packages have no \code{update.packages()} channel, so on launch
\code{run_app()} quietly checks the package repository for a newer shinytone
release (2-second timeout; skipped silently when offline). If one exists, a
reminder with the update command is printed to the console and shown once
inside the app. Disable the check with
\code{options(shinytone.check_updates = FALSE)}.
}
\examples{
\dontrun{
  shinytone::run_app()
  shinytone::run_app(port = 4321, launch.browser = FALSE)
}

}
