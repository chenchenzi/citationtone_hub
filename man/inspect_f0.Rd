% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inspect.R
\name{inspect_f0}
\alias{inspect_f0}
\title{Inspect f0 data for token-level outliers and sample-level jumps}
\usage{
inspect_f0(
  data,
  f0 = "f0",
  token = "token",
  time = "time",
  speaker = "speaker",
  tone = "tone",
  z_threshold = 3,
  rise_threshold = 1.263,
  fall_threshold = 1.714,
  octave_bounds = c(0.49, 1.99),
  carryover_mult = 1.5,
  level_threshold = 3.5,
  min_tokens = 5,
  intensity = NULL,
  intensity_drop = 15,
  time_unit = c("s", "ms")
)
}
\arguments{
\item{data}{A long-format data frame with one row per f0 sample.}

\item{f0}{Column name of f0 in Hz. Default \code{"f0"}.}

\item{token}{Column name of token ID. Default \code{"token"}.}

\item{time}{Column name of time. Default \code{"time"}.}

\item{speaker}{Column name of speaker ID. Default \code{"speaker"}.}

\item{tone}{Column name of tone category, or \code{NULL}. Default \code{"tone"}.
When \code{NULL}, the token-level (speaker-by-tone) level check
(\code{\link[=flag_level_outliers]{flag_level_outliers()}}) is skipped and the \code{tone} column is omitted
from the output, so the wrapper can run before tone categories are known
(e.g. the \code{\link[=cluster_f0]{cluster_f0()}} tone-discovery workflow); the speaker-level and
sample-level checks still run.}

\item{z_threshold}{Signed z-score cutoff: a token is flagged when its
per-token max exceeds \code{+z_threshold} (too high) or its min falls below
\code{-z_threshold} (too low). Default \code{3}.}

\item{rise_threshold}{Maximum plausible rise in ST per 10 ms.
Default \code{1.263} (Sundberg 1973).}

\item{fall_threshold}{Maximum plausible fall in ST per 10 ms.
Default \code{1.714} (Sundberg 1973).}

\item{octave_bounds}{Hz-ratio bounds outside which a step is flagged
as an octave jump. Default \code{c(0.49, 1.99)} (halving or doubling).}

\item{carryover_mult}{Carryover band as a multiple of the rise/fall
threshold (in semitones). \code{0} disables carryover. Default \code{1.5}.}

\item{level_threshold}{Absolute modified z-score above which a token's
level is flagged. Default \code{3.5} (Iglewicz & Hoaglin 1993).}

\item{min_tokens}{Minimum number of same-speaker-same-tone tokens
required to run the check for a group. Default \code{5}. More tokens give a
more reliable estimate of the group's spread.}

\item{intensity}{Optional column name of intensity in dB. When supplied,
the sample-level low-intensity check (\code{\link[=flag_low_intensity]{flag_low_intensity()}}) is run and
its results are added (see Value). \code{NULL} (default) skips the check.}

\item{intensity_drop}{Flag voiced samples whose intensity is more than this
many dB below the token's peak intensity. Default \code{15}. Ignored when
\code{intensity} is \code{NULL}.}

\item{time_unit}{One of \code{"s"} or \code{"ms"}. Default \code{"s"}. Inspection is
meant to run on real-time data; a normalised-time option was removed
because the physiological rate thresholds below lose their meaning once
real time is discarded.}
}
\value{
A long-format data frame containing the original \code{token},
\code{time}, \code{f0}, \code{speaker}, \code{tone} columns plus:
\itemize{
\item \code{f0_token_max}, \code{f0_token_min}, \code{f0_token_mean}, \code{f0_token_sd}:
per-token summary statistics.
\item \code{flagged_jump}: logical, sample-level jump flag.
\item \code{flagged_token}: logical, \code{TRUE} if the token has any extreme value,
any sample-level jump, or an unusual overall level for its tone.
\item \code{flag_notes}: human-readable concatenation of the reasons a sample
was flagged (e.g. \code{"max too high"}, \code{"jump (rise)"},
\code{"level too high"}, \code{"low intensity"}).
}

When \code{tone} is \code{NULL} the \code{tone} column is omitted and the level check is
not run, so \code{flag_notes} never contains \code{"level too high"} /
\code{"level too low"} and those checks do not contribute to \code{flagged_token}.

When \code{intensity} is supplied, the result additionally carries the
\code{intensity} column and a sample-level \code{flag_low_intensity} logical. The
low-intensity flag is advisory: it appears in \code{flag_notes} but does not by
itself set \code{flagged_token}.
}
\description{
One-call convenience wrapper that runs \code{\link[=flag_outliers]{flag_outliers()}},
\code{\link[=flag_pitch_jumps]{flag_pitch_jumps()}}, and (when \code{tone} is supplied) \code{\link[=flag_level_outliers]{flag_level_outliers()}}
on the same dataset, joins their results back into one long-format data
frame, and produces a single \code{flag_notes} column with human-readable
reasons for each flag. This is the function that backs the Inspect tab of
the Shiny app.
}
\details{
\subsection{What the function does internally}{
\enumerate{
\item Call \code{\link[=flag_outliers]{flag_outliers()}} for the speaker-level extreme-value check
(\code{flag_too_high}, \code{flag_too_low}) plus per-token summary statistics.
\item When \code{tone} is supplied, call \code{\link[=flag_level_outliers]{flag_level_outliers()}} for the
token-level (speaker-by-tone) level check (\code{flag_level_high},
\code{flag_level_low}). With \code{tone = NULL} this screen is skipped and both
flags are set to \code{FALSE}.
\item Call \code{\link[=flag_pitch_jumps]{flag_pitch_jumps()}} for the sample-level pitch-tracking artefact
flags (\code{flagged_jump}, \code{jump_note}).
\item Left-join the token-level flags onto the long-format jump output, so
every sample carries all three kinds of information.
\item Set \code{flagged_token} to \code{TRUE} for any token that is a speaker-level
extreme, an unusual level for its tone, or contains at least one
sample-level jump.
\item Concatenate the human-readable reasons into a single \code{flag_notes}
column for display.
}

Use the individual functions (\code{\link[=flag_outliers]{flag_outliers()}}, \code{\link[=flag_level_outliers]{flag_level_outliers()}},
\code{\link[=flag_pitch_jumps]{flag_pitch_jumps()}}) if you want only one kind of check or want to combine
the outputs yourself in a non-standard way.
}
}
\examples{
data(sample_f0)
result <- inspect_f0(sample_f0,
                     f0      = "f0_Hz",
                     token   = "token",
                     time    = "time",
                     speaker = "speaker",
                     tone    = "tone")

# How many tokens were flagged at default thresholds?
table(unique(result[, c("token", "flagged_token")])$flagged_token)

# Inspect the first few flagged samples
head(result[result$flagged_jump, c("token", "time", "f0_Hz", "flag_notes")])

}
\references{
Steffman, J., & Cole, J. (2022). An automated method for detecting f0
measurement jumps based on sample-to-sample differences. \emph{JASA
Express Letters}, 2(11), 115201. \doi{10.1121/10.0015045}

Sundberg, J. (1973). The acoustics of the singing voice.
\emph{Scientific American}, 229(3), 82–91.

Xu, C., & Zhang, C. (2024). A cross-linguistic review of citation
tone production studies: Methodology and recommendations.
\emph{The Journal of the Acoustical Society of America}, 156(4),
2538–2565. \doi{10.1121/10.0032356}
}
\seealso{
\itemize{
\item \code{\link[=flag_outliers]{flag_outliers()}}, \code{\link[=flag_pitch_jumps]{flag_pitch_jumps()}}, and \code{\link[=flag_level_outliers]{flag_level_outliers()}},
the three components.
\item \code{\link[=normalise_f0]{normalise_f0()}} for the downstream normalisation step.
}
}
