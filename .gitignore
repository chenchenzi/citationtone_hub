# R-specific files
.Rhistory
.RData
.Rproj.user
.Rproj

# Shiny log files
shiny.log

# macOS specific files
.DS_Store

# Cache and temporary files
.Rcache/
__pycache__/
*.RData
*.Rhistory

# Ignore the rsconnect deployment folder
rsconnect/
.claude/worktrees/

# pkgdown build output (deployed via GitHub Actions, not committed)
docs/

# Figure/poster rendering build artefacts (headless-Chrome / npm install)
node_modules/
.render/

# Talk slides are kept local, not committed
slides/

# macOS Finder custom-folder-icon artifact (name contains a CR); breaks
# rsconnect manifests if bundled
Icon?

# R fallback graphics device (headless plotting artefact)
Rplots.pdf
